---
// JAVASCRIPT FRONT MATTER

// Import the Layout for the page
import BaseLayout from "../layouts/BaseLayout.astro";

// Import smaller components
import BlogPost from "../components/BlogPost.astro";

// Astro API
const allPosts = await Astro.glob('../pages/posts/*.md');
console.log("Astro")

// Declare Variables
const pageTitle = "Blog";
---

<!-- The purpose of this page is to showcase my blogs -->

<BaseLayout pageTitle={pageTitle}>

    <p> 
        Welcome to my Blog, my little corner on the internet,
        where I document my learnings as I explore the world of Code, Design, and Animation 
        to create visually engaging, interactive, and immersive experiences.
        
        Every now and then I also discuss ideas in the realm of Philosophy and Creativity.
        Feel free to explore some of these ideas as well.
    </p>

    <p>This is where I will post about my journey learning Astro.</p>
    
    <ul class="blog-list">
        {
            allPosts.map((post) => {
                // return <li><a href={post.url}>{post.frontmatter.title}</a></li>
                return <li class="blog-list__item">
                            <BlogPost 
                                url={post.url} 
                                title={post.frontmatter.title} 
                                description={post.frontmatter.description} 
                                imageUrl={post.frontmatter.image.url}
                                tags = {post.frontmatter.tags}
                            />
                        </li>

            })

        }
    </ul>

</BaseLayout>

<style>

    .blog-list {

        /** Box Model */
        margin-top: 4rem;
        margin-inline: 3rem;

        /** Layout **/
        display: grid;
        grid-template-rows: repeat(2, 1fr);;
        grid-template-columns: repeat(1, 1fr);;
        gap: 2rem;
    
    }

    .blog-list__item {
       list-style: none;
    }


</style>
